<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi perfil — HelpDesk IntegralTI</title>
    <link rel="stylesheet" href="/css/estilos.css">
</head>
<body>
    <nav class="navbar">
        <span class="navbar-marca">HelpDesk IntegralTI</span>
        <div class="navbar-links">
            <a href="/tecnico/tickets">Todos los tickets</a>
            <a href="/tecnico/categorias">Categorías</a>
            <a href="/tecnico/perfil" class="activo">Mi perfil</a>
            <a href="/logout">Cerrar sesión</a>
        </div>
        <span class="navbar-usuario"><%= usuario.nombre %> — Técnico</span>
    </nav>

    <main class="contenedor">
        <h1 class="titulo-seccion">Mi perfil</h1>

        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>
        <% if (exito) { %>
            <p class="exito"><%= exito %></p>
        <% } %>

        <div class="panel-perfil">

            <!-- Datos personales -->
            <div class="tarjeta-detalle">
                <h2 class="subtitulo-seccion">Datos personales</h2>
                <div class="info-perfil">
                    <p><span class="etiqueta">Email</span> <%= usuario.email %></p>
                    <p><span class="etiqueta">Rol</span> <%= usuario.nombre_rol %></p>
                    <p><span class="etiqueta">Departamento</span> <%= usuario.nombre_departamento %></p>
                </div>
                <form method="POST" action="/tecnico/perfil/datos" class="formulario" style="margin-top:1.5rem">
                    <div class="fila-campos">
                        <div class="campo">
                            <label for="nombre">Nombre</label>
                            <input type="text" id="nombre" name="nombre" 
                                value="<%= usuario.nombre %>" required>
                        </div>
                        <div class="campo">
                            <label for="apellido">Apellido</label>
                            <input type="text" id="apellido" name="apellido" 
                                value="<%= usuario.apellido %>" required>
                        </div>
                    </div>
                    <button type="submit" class="btn-primario">Guardar cambios</button>
                </form>
            </div>

            <!-- Cambiar contraseña -->
            <div class="tarjeta-detalle">
                <h2 class="subtitulo-seccion">Cambiar contraseña</h2>
                <form method="POST" action="/tecnico/perfil/password" class="formulario">
                    <div class="campo">
                        <label for="password_actual">Contraseña actual</label>
                        <input type="password" id="password_actual" name="password_actual" required>
                    </div>
                    <div class="campo">
                        <label for="password_nuevo">Nueva contraseña</label>
                        <input type="password" id="password_nuevo" name="password_nuevo" required>
                    </div>
                    <div class="campo">
                        <label for="password_confirmar">Confirmar nueva contraseña</label>
                        <input type="password" id="password_confirmar" name="password_confirmar" required>
                    </div>
                    <button type="submit" class="btn-primario">Cambiar contraseña</button>
                </form>
            </div>

        </div>
    </main>
</body>
</html>
